package com.zentu.zentu_core.billing.enums;

public enum TransactionStatus {
    PENDING,
    COMPLETED,
    FAILED,
    REVERSED,
    CANCELLED,
    IN_PROGRESS,
    ON_HOLD,
    EXPIRED,
    DECLINED
}
